{
  "name": "proxy",
  "version": "1.0.0",
  "description": "Reverse proxy server that caches the content in the local disk",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "server": "nodemon -w lib/ -w server.js -w config.js server.js",
    "simple": "nodemon -w lib/ -w simple.js -w config.js simple.js"
  },
  "author": "gopicode1@gmail.com",
  "license": "MIT",
  "dependencies": {
    "agentkeepalive": "^3.1.0",
    "debug": "^2.6.8",
    "mime": "^1.3.6",
    "mkdirp": "^0.5.1",
    "rimraf": "^2.6.1"
  }
}
